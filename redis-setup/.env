# ==========================================
# IDM-GridCore Redis 配置
# ==========================================

# ---------- 安全设置（必须修改！）----------
# Redis 访问密码，必须设置一个强密码
# 建议：20位以上，包含大小写字母、数字、特殊符号
REDIS_PASSWORD=your-strong-password-here

# ---------- 内存设置 ----------
# Redis 最大内存使用量（根据服务器内存调整）
# 建议设置为服务器内存的 1/4 到 1/2
REDIS_MAXMEMORY=512mb

# 内存淘汰策略（默认 allkeys-lru）
# 可选值：
#   allkeys-lru     - 最近最少使用的 key 被淘汰（推荐）
#   allkeys-lfu     - 使用频率最少的 key 被淘汰
#   volatile-lru    - 只淘汰设置了过期时间的 key（LRU）
#   noeviction      - 不淘汰，内存满时返回错误
# REDIS_MAXMEMORY_POLICY=allkeys-lru

# ---------- 网络设置（host 模式下无效）----------
# 如需使用端口映射模式，修改映射端口
# REDIS_PORT=6379
